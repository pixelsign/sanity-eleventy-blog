---
layout: layouts/base.njk
templateClass: tmpl-post
---
{% set imageObject = { url: post.mainImage.url, alt: post.mainImage.alt } %}
<article>
  <h1>{{ post.title }}</h1>
  <img src="{% imageUrlFor post.mainImage, 600 %}" alt="{{ post.mainImage.alt }}">
  <p>
  Written by {% for author in post.authors %}
  <a href="/authors/{{ author.slug.current | slug }}/"> {{author.name}} </a>
  {% endfor %}
</p>
  {{ post.body | markdownify | safe }}
  <p><a href="{{ '/' | url }}">← Home</a></p>
</article>

